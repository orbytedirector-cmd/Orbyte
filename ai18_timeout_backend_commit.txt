Fix (backend): reducir timeout por proveedor de IA a 8s (Ticket AI-18)

Diagnosticado cruzando log de dispositivo (con el logueo de AI-17 ya
funcionando) + log de servidor: /api/v1/ai/playlist timeouteaba tanto
por LAN como por Tailscale del lado del cliente, y el servidor nunca lo
registraba porque nunca terminaba de responder a tiempo.

_HTTP_TIMEOUT_SECONDS baja de 12 a 8. Peor caso: 2 proveedores (Gemini,
si falla, Groq) x 8s + overhead de DB ~= 17s, contra los ~24s+ de antes.
Ver Ticket AI-18 del lado iOS (OrbyteApiClient.swift) para los timeouts
de cliente nuevos (30s), calculados con margen real sobre este número.

Validado: py_compile OK. git apply --check OK en clon independiente,
aplicado y recompilado en clon de verificación.

Único archivo: ai_playlist.py (1 constante + comentario).
