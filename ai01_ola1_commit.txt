Feat (backend): agente de IA — intent parser + filter mapper (Ticket AI-01, primera ola)

Agrega POST /api/v1/ai/playlist: recibe una petición en lenguaje natural,
la interpreta vía LLM (Gemini primario, Groq como respaldo) y arma una
playlist reutilizando _build_adv_filters/_track_dedupe_condition/
track_to_json ya existentes, sin modificarlos. Si no hay resultados o el
LLM falla, aplica relajación progresiva de filtros (idioma -> pais -> era
-> tema -> momento -> mood -> genero) y, como último recurso, la playlist
más popular sin filtros — nunca devuelve una lista vacía si hay al menos
una pista en la biblioteca.

Nuevo: ai_playlist.py (módulo aislado, no importa app.py — recibe las
funciones que necesita por parámetro). Tabla nueva ai_playlist_requests
(lazy migration, mismo patrón que el resto de la base) para loguear cada
petición: texto crudo, intención parseada, filtros aplicados, si se usó
fallback, proveedor y confianza — insumo para iterar prompts más adelante.

start.sh: se agrega 'requests' a las dependencias instaladas (ya se
importaba de forma opcional en app.py, pero nunca estaba en esa lista).

Variables de entorno nuevas, todas opcionales (ver ticket §3):
GEMINI_API_KEY, GEMINI_MODEL, GROQ_API_KEY, GROQ_MODEL. Sin ninguna
seteada, el endpoint no falla — cae directo al fallback de popularidad.

Validado: py_compile OK, git apply --check OK en clon independiente,
aplicado y recompilado en clon de verificación separado.

Etapas 3+4 de AI_AGENT_MASTER_PLAN.md — primera ola del epic Agente de IA.
