diff --git a/templates/album.html b/templates/album.html
index d7df2b1..e41c066 100755
--- a/templates/album.html
+++ b/templates/album.html
@@ -313,6 +313,7 @@
 
   // Prepend all album tracks above current queue position and start playing from the first
   async function playAllAlbum() {
+    if (window.primeAudioForGesture) window.primeAudioForGesture();
     const q = await ensureAlbumQueue();
     if (!q || !q.length) { console.warn('playAllAlbum: empty queue'); return; }
     if (window.prependTracksAndPlay) {
@@ -325,6 +326,7 @@
 
   // Play a single track: prepend it to the top of the queue and start it immediately
   async function playTrackFromAlbum(trackId) {
+    if (window.primeAudioForGesture) window.primeAudioForGesture();
     try {
       const data = await fetch(`/api/track/${trackId}`).then(r => r.json());
       if (window.prependAndPlay) {
diff --git a/templates/artist.html b/templates/artist.html
index 0fe6649..cd9902c 100755
--- a/templates/artist.html
+++ b/templates/artist.html
@@ -200,6 +200,7 @@
 
 <script>
 async function playAlbumById(albumId, el) {
+  if (window.primeAudioForGesture) window.primeAudioForGesture();
   if (el) { const o = el.querySelector('.play-overlay'); if (o) o.textContent = '…'; }
   try {
     const tracks = await fetch(`/api/album/${albumId}/tracks`).then(r => r.json());
diff --git a/templates/browse.html b/templates/browse.html
index 55ba130..6d6720c 100755
--- a/templates/browse.html
+++ b/templates/browse.html
@@ -323,6 +323,7 @@ function _tracksViewActive() {
 
 // ── Album play ─────────────────────────────────────────────────────────────────
 async function playAlbumById(id, el) {
+  if (window.primeAudioForGesture) window.primeAudioForGesture();
   const overlay = el ? el.querySelector('.play-overlay') : null;
   if (overlay) overlay.textContent = '…';
   try {
@@ -340,6 +341,7 @@ window.playAlbumById = playAlbumById;
 
 // ── Play/queue everything currently loaded — funciona en Álbumes y en Pistas ──
 window.playAllLoaded = async function() {
+  if (window.primeAudioForGesture) window.primeAudioForGesture();
   if (IS_META && _tracksViewActive() && metaTracksQueue.length) {
     if (window.prependTracksAndPlay) { window.prependTracksAndPlay(metaTracksQueue, 0); }
     else { loadQueue(metaTracksQueue); playTrack(0); }
diff --git a/templates/home.html b/templates/home.html
index 8c99ae5..f3d8514 100755
--- a/templates/home.html
+++ b/templates/home.html
@@ -406,6 +406,7 @@ function filterGenres(q) {
   });
 }
 async function playAlbumById(albumId) {
+  if (window.primeAudioForGesture) window.primeAudioForGesture();
   const tracks = await fetch(`/api/album/${albumId}/tracks`).then(r => r.json());
   if (!Array.isArray(tracks) || !tracks.length) return;
   const valid = tracks.filter(t => t.audio_url || t.file_path);
diff --git a/templates/search.html b/templates/search.html
index ade4f35..00515a5 100755
--- a/templates/search.html
+++ b/templates/search.html
@@ -134,6 +134,7 @@ function showTab(panel, btn) {
   btn.classList.add('active');
 }
 async function playTrackById(id) {
+  if (window.primeAudioForGesture) window.primeAudioForGesture();
   const d = await fetch(`/api/track/${id}`).then(r=>r.json());
   if (window.prependAndPlay) { window.prependAndPlay(d); }
   else { loadQueue([d]); playTrack(0); }
@@ -161,6 +162,7 @@ async function addToPlaylistByIdWithFeedback(id, btn) {
   }
 }
 async function playAlbumById(id) {
+  if (window.primeAudioForGesture) window.primeAudioForGesture();
   const tracks = await fetch(`/api/album/${id}/tracks`).then(r=>r.json());
   if (!Array.isArray(tracks) || !tracks.length) return;
   const valid = tracks.filter(t => t.audio_url || t.file_path);
