diff --git a/templates/home.html b/templates/home.html
index da5205b..8c99ae5 100755
--- a/templates/home.html
+++ b/templates/home.html
@@ -11,13 +11,13 @@
 </div>
 
 <!-- Favorites quick access -->
-<div style="margin-bottom:1.5rem;padding:1rem 1.25rem;background:var(--bg-card);border:1px solid var(--border);border-radius:12px;display:flex;align-items:center;gap:1rem;flex-wrap:wrap">
-  <span style="font-size:1.4rem">❤️</span>
-  <div style="flex:1">
-    <div style="font-weight:700;font-size:1rem;color:var(--text-primary)">Tus Favoritos</div>
-    <div style="font-size:0.8rem;color:var(--text-muted)" id="fav-count-home">Cargando...</div>
+<div class="fav-banner">
+  <span class="fav-banner-icon">❤️</span>
+  <div class="fav-banner-text">
+    <div class="fav-banner-title">Tus Favoritos</div>
+    <div class="fav-banner-sub" id="fav-count-home">Cargando...</div>
   </div>
-  <a href="/favorites" class="track-btn ghost" style="text-decoration:none">Ver favoritos →</a>
+  <a href="/favorites" class="track-btn ghost fav-banner-cta">Ver favoritos →</a>
 </div>
 <script>
   fetch('/api/favorites').then(r=>r.json()).then(d=>{
@@ -258,6 +258,14 @@
 </div>
 
 <style>
+/* Favorites banner (antes inline styles en el div) */
+.fav-banner { margin-bottom:1.5rem; padding:1rem 1.25rem; background:var(--bg-card); border:1px solid var(--border); border-radius:12px; display:flex; align-items:center; gap:1rem; flex-wrap:wrap; }
+.fav-banner-icon { font-size:1.4rem; }
+.fav-banner-text { flex:1; min-width:0; }
+.fav-banner-title { font-weight:700; font-size:1rem; color:var(--text-primary); }
+.fav-banner-sub { font-size:0.8rem; color:var(--text-muted); }
+.fav-banner-cta { text-decoration:none; }
+
 /* Quality grid */
 .quality-grid { display:grid; grid-template-columns:1fr 1fr; gap:0.35rem; }
 .quality-card { display:flex; align-items:center; gap:0.75rem; padding:0.5rem 0.85rem; background:var(--bg-card); border:1px solid var(--border); border-radius:10px; text-decoration:none; color:inherit; transition:border-color 0.2s,background 0.2s; }
@@ -341,6 +349,44 @@
 .album-card-meta-row { display:flex; align-items:center; justify-content:center; gap:0.25rem; font-size:0.72rem; color:rgba(200,146,10,0.55); margin-top:0.2rem; flex-wrap:wrap; }
 .album-card-meta-row .meta-year { color:var(--accent); }
 .meta-sep { color:rgba(200,146,10,0.3); }
+
+/* ═══════════════════════════════════════════════════════════════════════
+   RESPONSIVE — Home ≤768px
+   Estas grillas viven solo en este template (no están en style.css), y no
+   tenían ningún tratamiento mobile. .quality-grid (2 columnas fijas) y
+   .era-card (columna central de 195px fija) no entraban en pantallas
+   angostas y forzaban scroll horizontal de toda la página — se ve en las
+   capturas adjuntas como texto y badges cortados en el borde derecho.
+   ═══════════════════════════════════════════════════════════════════════ */
+@media (max-width: 768px) {
+  /* Banner de favoritos: el botón pasa a ocupar una fila propia, full-width */
+  .fav-banner { padding: 0.85rem 1rem; gap: 0.6rem; }
+  .fav-banner-cta { flex: 1 1 100%; text-align: center; }
+
+  /* Distribución de calidad: 1 columna en vez de 2 fijas */
+  .quality-grid { grid-template-columns: 1fr; gap: 0.4rem; }
+
+  /* Momento del día / idioma: tarjetas un poco más chicas para que entren
+     3 por fila en vez de 2 apretadas */
+  .momento-grid, .language-grid { grid-template-columns: repeat(auto-fill, minmax(88px,1fr)); }
+
+  /* Era musical: 1 columna, y la tarjeta pasa de una grilla con columna
+     central fija (55px 195px 1fr 58px, pensada para desktop) a un layout
+     de 2 filas: [década | etiqueta | contador] arriba, barra abajo. */
+  .era-timeline { grid-template-columns: 1fr; gap: 0.4rem; }
+  .era-card {
+    grid-template-columns: 40px 1fr auto;
+    grid-template-areas:
+      "decade label count"
+      "bar    bar   bar";
+    row-gap: 0.35rem;
+    padding: 0.55rem 0.85rem;
+  }
+  .era-card .ec-decade   { grid-area: decade; }
+  .era-card .ec-label    { grid-area: label; min-width: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
+  .era-card .ec-count    { grid-area: count; }
+  .era-card .ec-bar-wrap { grid-area: bar; }
+}
 </style>
 
 <script>
