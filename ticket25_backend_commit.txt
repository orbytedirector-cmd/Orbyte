Fix (backend): comprimir log de reporte de bugs automáticamente (Ticket 25)

Pedido por Niko: adjuntar el log completo del dispositivo al correo de
reporte de bugs, comprimiendo si hace falta para que el tamaño nunca sea
un problema.

/api/v1/report-issue: en vez de comprimir solo cuando ya "es un
problema" (umbral arbitrario), se comprime siempre que valga la pena
(>50KB) -- texto de log comprime 90%+ típico, gzip es prácticamente
gratis en CPU. Log <=50KB se adjunta tal cual (log.txt), log >50KB se
comprime (log.txt.gz). Probado con texto de log real: 186KB -> 632 bytes
(99.7% de reducción).

De paso: timeout de conexión SMTP 10s -> 20s, margen extra ahora que los
adjuntos pueden ser (levemente) más grandes.

Validado: py_compile OK, prueba real de compresión con gzip. git apply
--check OK en clon independiente, aplicado y recompilado en clon de
verificación.

Único archivo: app.py.
